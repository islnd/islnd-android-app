<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/invite_vertical_padding"
    android:paddingBottom="@dimen/invite_vertical_padding"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin">

    <TextView
        android:id="@+id/invite_display_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/invite_content_margin"
        android:layout_marginRight="@dimen/invite_content_margin"
        style="@style/Base.TextAppearance.AppCompat.Medium"
        android:textColor="@color/primary_text"/>

    <TextView
        android:id="@+id/invite_phone_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/invite_content_margin"
        android:layout_toRightOf="@id/invite_display_name"
        style="@style/Base.TextAppearance.AppCompat.Medium"
        android:textColor="@color/primary_text"/>

</RelativeLayout>