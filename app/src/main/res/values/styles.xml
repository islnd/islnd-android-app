<resources>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="itemBackground">@color/background_mat</item>
        <item name="itemTextAppearance">@style/AppTheme.PopupText</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat">
        <item name="android:colorBackground">@color/background_mat</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="AppTheme.TransparentToolbar">
        <item name="titleTextColor">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
        <item name="background">@android:color/transparent</item>
    </style>

    <style name="IslndCrop" parent="Crop">
        <item name="showThirds">true</item>
        <item name="showCircle">true</item>
        <item name="showHandles">always</item>
        <item name="highlightColor">@android:color/white</item>
    </style>

    <style name="AppTheme.Dialog" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="background">@color/background_mat</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="textColorAlertDialogListItem">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
    </style>

    <style name="AppTheme.PopupText">
        <item name="android:textColor">@color/primary_text</item>
    </style>

</resources>
